## 1. 配置与数据模型

- [x] 1.1 新增地图阶段配置，覆盖 `起始区域 I` 到 `高危区域 II`、`时刻 1` 到 `时刻 8-4`，并标明等级范围、解锁条件、入口消耗和掉落 profile。
- [x] 1.2 新增怪物等级曲线配置，定义各阶段 monster level、生命倍率、伤害倍率和奖励等级加成。
- [x] 1.3 新增通用 loot profile 配置，定义装备、宝石、地图入口掉落率、数量倍率、稀有度权重和 Boss 保底。
- [x] 1.4 新增地图入口配置，定义同层、低一层、高一层入口掉落规则，并保证 `起始区域 I` 免费无限刷。
- [x] 1.5 新增或扩展后端数据结构：`MapRunContext`、`LootContext`、`DroppedLoot`、`MapEntry` 和地图进度状态。
- [x] 1.6 添加配置加载和校验测试，覆盖阶段 ID 唯一、等级范围连续、免费起始区、入口引用合法和 loot profile 引用合法。

## 2. 后端地图运行时

- [x] 2.1 实现后端 map run 创建流程，从地图选择请求生成 canonical `MapRunContext`。
- [x] 2.2 将现有怪物定义和怪物包结果适配为后端 canonical monster instances。
- [x] 2.3 按 map run context 计算怪物生命和伤害，并复用现有 normal/magic/rare/boss 稀有度倍率。
- [x] 2.4 保留或迁移现有前端生怪结果为渲染适配，不允许其决定真实 HP、伤害、稀有度或掉落。
- [x] 2.5 添加测试证明 playable map-run path 使用后端 canonical monster instances。

## 3. 通用掉落系统

- [x] 3.1 将 `LootRuntime` 从 gem-only 输出扩展为通用 `DroppedLoot` 输出，支持零掉落和多掉落。
- [x] 3.2 实现装备掉落，选择装备 source、item level、rarity，并调用 `EquipmentGenerator.generate(source, item_level, rarity)`。
- [x] 3.3 实现宝石等级掉落，按阶段配置生成 Lv1-Lv20 玩家可见宝石实例。
- [x] 3.4 实现地图入口掉落，支持 `起始区域 I` 掉 `起始区域 II`、中后期掉同层/低一层/高一层入口。
- [x] 3.5 将玩家掉落数量和稀有度属性接入通用掉落计算。
- [x] 3.6 添加后端测试覆盖零掉落、装备掉落、宝石等级、地图入口、Boss 保底和 rare/Boss 掉落倍率。

## 4. 战斗与拾取接入

- [x] 4.1 将 `CombatSession` 的击杀掉落调用改为传入当前 `MapRunContext` 和 monster loot context。
- [x] 4.2 将 `DroppedGem` 兼容迁移到通用掉落 presentation/API，不破坏现有宝石拾取行为。
- [x] 4.3 实现装备拾取入装备背包，并保留 source、level、rarity、base affix 和 ordinary affixes。
- [x] 4.4 实现地图入口拾取入地图入口库存或地图选择资源状态。
- [x] 4.5 添加测试覆盖战斗击杀后生成通用掉落、拾取宝石、拾取装备、拾取地图入口和零掉落不报错。

## 5. 自动存档

- [x] 5.1 设计并实现带版本号的单槽自动存档结构。
- [x] 5.2 持久化已解锁地图、地图入口库存、GemInventory、装备列表、装备槽、数独盘、当前地图选择和最近安全状态。
- [x] 5.3 在进入地图、击杀产生掉落、拾取、装备/卸装、宝石上盘/下盘、地图解锁和返回主界面时触发自动保存。
- [x] 5.4 实现继续游戏和新游戏加载路径，缺字段存档使用安全默认值补齐。
- [x] 5.5 添加测试覆盖新存档创建、自动保存触发、继续游戏恢复、旧存档兼容和断票仍可进入 `起始区域 I`。

## 6. 主界面与地图选择 UI

- [x] 6.1 新增 WebApp 主界面，提供继续游戏、新游戏、地图选择和当前存档摘要。
- [x] 6.2 新增地图选择界面，展示成长地图阶段、异界时刻阶段、已解锁/未解锁/入口不足/免费状态。
- [x] 6.3 在地图选择详情展示地图等级、怪物等级、装备 tier 提示、宝石等级范围、地图入口产出和 Boss 奖励提示。
- [x] 6.4 从地图选择启动后端 map run，并让 playable battle view 渲染后端 canonical 地图、怪物、战斗和掉落状态。
- [x] 6.5 更新掉落展示和拾取 UI，区分宝石、装备和地图入口，并使用中文玩家可见文本。
- [x] 6.6 添加前端/API 测试或 smoke test，覆盖主界面、地图选择、免费起始区、入口不足提示、进入地图和通用掉落展示。

## 7. 边界清理与回归

- [x] 7.1 确认 playable WebApp battle path 不使用前端本地逻辑计算真实怪物 HP、伤害、目标选择、掉落或奖励等级。
- [x] 7.2 确认装备掉落没有复制装备词缀 tier、容量、进阶或至臻规则。
- [x] 7.3 确认地图入口系统不引入通货、交易、赛季、Atlas 或复杂地图词缀。
- [x] 7.4 确认 skill editor、`/skill-editor`、`dist-skill-editor` 和端口 `8765` 未被用于验证。
- [x] 7.5 确认截图、日志和生成证据不写入仓库根目录。

## 8. 验证

- [x] 8.1 运行聚焦后端测试：地图阶段、怪物数值、通用掉落、装备掉落、宝石等级、地图入口、自动存档和 Web API。
- [x] 8.2 运行现有装备、宝石、技能、战斗和配置校验测试，确认刷宝改动没有破坏已有能力。
- [x] 8.3 运行 WebApp 构建或 smoke test。
- [x] 8.4 启动 playable WebApp battle view，从主界面进入地图选择，进入 `起始区域 I`，击杀怪物并观察通用掉落。
- [x] 8.5 捕获主界面、地图选择和 playable battle drop 验证截图到 `artifacts/screenshots/`，并描述截图中可见内容。
