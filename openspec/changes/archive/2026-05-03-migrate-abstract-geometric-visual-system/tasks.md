## 1. Phase 1：视觉 tokens 与 Canvas 基础层

- [x] 1.1 新增全局几何视觉 tokens，定义深色哑光背景、白/橙/蓝/灰主色、UI 字号、边框、透明度和动效时长。
- [x] 1.2 新增 Canvas 战斗几何 renderer 骨架，支持按设备像素比自适应尺寸、清屏、镜头变换、背景网格和基础几何标记绘制。
- [x] 1.3 在主战斗场景接入 Canvas 几何层，传入只读战斗快照，不改变玩家移动、敌人 AI、技能释放、伤害、掉落或数独规则。
- [x] 1.4 增加 Phase 1 smoke 验证：构建通过，战斗画面非空，Canvas 层存在，旧 DOM 战斗画面仍可运行。

## 2. Phase 2：实体与投射物表现替换

- [x] 2.1 用 Canvas 几何绘制玩家、普通怪、精英怪和 Boss，保留现有世界坐标、血量和深度排序输入。
- [x] 2.2 用 Canvas 几何绘制投射物、弹道、尾迹和投射物阴影，旧 PNG VFX 仅保留 fallback。
- [x] 2.3 验证 300 个敌人与 1000 个投射物压力场景下，战斗对象不创建大量 DOM 节点。
- [x] 2.4 新增首批几何怪物定义与怪物组配置，使用普通/魔法/史诗/BOSS 分层编号，并验证同组成员保持同类型。
- [x] 2.5 调整几何怪物视觉配置：主体颜色统一为局内标准色，稀有度改由脚下底座表达，并按普通/魔法/稀有/BOSS 标准尺寸递增。

## 3. Phase 3：技能特效表现替换

- [x] 3.1 用 Canvas 绘制范围圈、nova、近战弧、damage zone、连锁线段和命中反馈。
- [x] 3.2 用 Canvas 绘制爆炸、地刺、短时粒子和伤害数字，避免大量 CSS filter、blur、box-shadow。
- [x] 3.3 用 Canvas 批量绘制编辑器运行地图 tile 几何表现层，复用现有 `empty / ground / wall` 数据、碰撞、出生点和遭遇点逻辑。
- [ ] 3.4 验证火焰、冰霜、闪电、物理、连锁、地刺和爆炸技能截图可读。

## 4. Phase 4：UI 几何极简皮肤

- [x] 4.1 重做 HUD、底部技能栏、战斗日志和调试文字样式，统一为深色哑光细边框 UI。
- [x] 4.2 重做背包、数独宝石盘、支持连线、tooltip 和拖拽状态视觉，不改变放置与交换规则。
- [x] 4.3 重做角色信息面板、属性图标、抗性图标和宝石图标为简洁几何图标系统。

## 5. Phase 5：旧视觉资源降级与验收

- [ ] 5.1 删除或降级旧像素角色、旧 PNG VFX、旧地牢/工作台素材的最终视觉依赖，保留必要 fallback。
- [ ] 5.2 补充最终截图验收：战斗常态、高密度战斗、技能爆发、背包+数独盘、角色面板、调试辅助线。
- [ ] 5.3 运行构建、smoke test 与相关单元测试，确认玩法逻辑未因视觉迁移改变。
